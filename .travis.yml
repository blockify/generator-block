language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-block' ]; then cd .. && eval "mv $currentfolder generator-block" && cd generator-block; fi

